# JEP 406 : Pattern Matching for switch (Preview)

if (shape instanceof Square square) // square est ce qu'on appelle une binding variable

if (o instanceof Rectangle(int width, int height)) { // déconstruction d'un record, on récupère deux binding variables

